# Check if node is installed and it's version
## Install if not
## Check if nvm is installed. Install if not
### Ask to install node if not using nvm

# Check if already has a package-lock.json and node_modules folder
## Install dependencies if not
npm install

# Check if vscode is installed. Recommend with winget command if not.
# Ask to open VSCode. Proccess if yes, go to next if not.

# Give recommendation command to run project.
npm run dev
